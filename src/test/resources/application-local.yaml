spring:
  datasource:
    url: jdbc:tc:postgresql:11:///testdata?TC_REUSABLE=true
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

env:
  services:
    library:
      url: http://localhost:8080